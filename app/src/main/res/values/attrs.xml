<resources>

    <!-- Declare custom theme attributes that allow changing which styles are
         used for button bars depending on the API level.
         ?android:attr/buttonBarStyle is new as of API 11 so this is
         necessary to support previous API levels. -->
    <declare-styleable name="ButtonBarContainerTheme">
        <attr name="metaButtonBarStyle" format="reference" />
        <attr name="metaButtonBarButtonStyle" format="reference" />
    </declare-styleable>


    <!-- hardware support indicator -->
    <declare-styleable name="HarewareInfoIndicator">
        <attr name="indicator_value" format="integer" />
        <!--<attr name="indicator_unsupport" format="integer" />-->
        <!--<attr name="indicator_support" format="integer" />-->
        <!--<attr name="indicator_good" format="integer" />-->
        <!--<attr name="indicator_abnormal" format="integer" />-->
        <!--<attr name="indicator_warning" format="integer" />-->
        <!--<attr name="indicator_inactive" format="integer" />-->
    </declare-styleable>

</resources>
